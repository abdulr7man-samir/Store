# 👗 متجر الأزياء الفاخرة - Fashion Store

متجر أزياء احترافي كامل بالعربية واللهجة المصرية، مع نظام دفع عند الاستلام وإدارة المنتجات والطلبات.

## ✨ المميزات

- ✅ واجهة عربية احترافية (اللهجة المصرية)
- ✅ إضافة المنتجات من لوحة التحكم
- ✅ سلة تسوق ديناميكية
- ✅ نظام الطلبات مع الدفع عند الاستلام
- ✅ المصادقة الآمنة مع Supabase
- ✅ رفع الصور مباشرة إلى Supabase Storage
- ✅ تصميم متجاوب يعمل على جميع الأجهزة
- ✅ تخزين البيانات في قاعدة بيانات Supabase

## 🛠️ التكنولوجيا المستخدمة

- **Next.js 14** - Framework React الحديث
- **Tailwind CSS** - تصميم واجهات المستخدم
- **Supabase** - Backend وقاعدة البيانات
- **React Hooks** - إدارة الحالة

## 📦 المتطلبات

- Node.js 18+
- حساب Supabase مجاني
- npm أو yarn

## 🚀 خطوات التثبيت والتشغيل

### 1️⃣ إنشاء حساب Supabase

1. اذهب إلى [https://supabase.com](https://supabase.com)
2. سجل حسابًا جديدًا (مجانًا)
3. أنشئ مشروعًا جديدًا (Choose Egypt region)
4. انسخ **Project URL** و **anon key** من Settings > API

### 2️⃣ إعداد قاعدة البيانات

افتح **SQL Editor** في Supabase وشغّل هذا الأمر:

```sql
-- جدول المنتجات
CREATE TABLE products (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  price INTEGER NOT NULL,
  image TEXT,
  created_at TIMESTAMP DEFAULT now()
);

-- جدول الطلبات
CREATE TABLE orders (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL,
  customer_name TEXT NOT NULL,
  customer_phone TEXT NOT NULL,
  customer_address TEXT NOT NULL,
  customer_city TEXT NOT NULL,
  total_price INTEGER NOT NULL,
  items TEXT NOT NULL,
  status TEXT DEFAULT 'انتظار التحضير',
  payment_method TEXT DEFAULT 'دفع عند الاستلام',
  created_at TIMESTAMP DEFAULT now()
);

-- السماح بالقراءة العامة للمنتجات
ALTER TABLE products ENABLE ROW LEVEL SECURITY;
CREATE POLICY "anyone_can_read_products" ON products
  FOR SELECT USING (true);

-- السماح للمستخدمين بقراءة طلباتهم
ALTER TABLE orders ENABLE ROW LEVEL SECURITY;
CREATE POLICY "users_can_read_own_orders" ON orders
  FOR SELECT USING (auth.uid() = user_id);

-- السماح بإنشاء طلبات جديدة
CREATE POLICY "users_can_create_orders" ON orders
  FOR INSERT WITH CHECK (auth.uid() = user_id);
```

### 3️⃣ إنشاء Storage Bucket

1. في Supabase، اذهب إلى **Storage**
2. أنشئ bucket جديد باسم `products`
3. اضبط الإعدادات:
   - Public access: **ON**
   - الملفات المسموحة: صور فقط

### 4️⃣ تثبيت المشروع

```bash
# انسخ الملفات واذهب للمجلد
cd fashion-store

# ثبت الحزم
npm install

# أنشئ ملف .env وأضف البيانات
cp .env.example .env.local
```

### 5️⃣ تعديل ملف .env

فتح `.env` وأضف بيانات Supabase:

```
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_KEY=your_anon_key_here
```

### 6️⃣ تشغيل المشروع

```bash
npm run dev
```

الموقع سيظهر على: **http://localhost:3000** 🎉

## 📱 الصفحات الرئيسية

| الصفحة | الرابط | الشرح |
|------|--------|-------|
| الرئيسية | `/` | الصفحة الرئيسية |
| المنتجات | `/products` | عرض كل المنتجات |
| السلة | `/cart` | عرض السلة |
| الدفع | `/checkout` | إتمام الطلب |
| الطلبات | `/orders` | الطلبات السابقة |
| التسجيل | `/auth` | تسجيل دخول/تسجيل |
| إضافة منتج | `/admin/products` | لوحة التحكم ⭐ |

## 🔐 الدخول إلى لوحة التحكم

1. اذهب إلى `/auth`
2. اختر "إنشاء حساب جديد"
3. استخدم بريد إلكتروني وكلمة سرية قوية
4. سجل دخول
5. الآن تقدري تضيفي منتجات جديدة! ✨

## 🎨 تخصيص العلامات التجارية

لتغيير اسم المتجر واللون:

### في `app/layout.js`:
```javascript
<h1 className="text-2xl font-bold">👗 متجر الأزياء الفاخرة</h1>
```

### الألوان في `tailwind.config.js`:
```javascript
colors: {
  primary: '#ff6b9d',    // اللون الوردي
  secondary: '#feca57',  // اللون الأصفر
}
```

## 📊 نموذج البيانات

### جدول المنتجات
```
id: string (UUID)
name: string - اسم المنتج
price: number - السعر بالجنيه
image: string - رابط الصورة
created_at: timestamp - تاريخ الإضافة
```

### جدول الطلبات
```
id: string (UUID)
user_id: string - ID المستخدم
customer_name: string - اسم الزبون
customer_phone: string - رقم الموبايل
customer_address: string - العنوان
customer_city: string - المحافظة
total_price: number - الإجمالي
items: JSON - المنتجات المشتراة
status: string - حالة الطلب
payment_method: string - طريقة الدفع
created_at: timestamp - وقت الطلب
```

## 💡 كيفية الاستخدام

### إضافة منتج جديد:
1. سجلي دخول
2. اذهبي إلى لوحة التحكم
3. أملئي البيانات (اسم + سعر + صورة)
4. اضغطي "إضافة المنتج"

### عملية الشراء:
1. تصفحي المنتجات
2. أضيفي للسلة
3. عدّلي الكميات حسب الحاجة
4. اضغطي "الدفع عند الاستلام"
5. أملئي بيناتك
6. أكملي الطلب
7. سيصلك في 1-3 أيام

## 🚀 النشر على الإنترنت

### على Vercel (الأفضل):
```bash
vercel
```

### على Replit:
1. اضغط "Import from GitHub"
2. داخل Replit:
   ```bash
   npm install
   npm run dev
   ```
3. شغّل الموقع!

## 🐛 استكشاف الأخطاء

### المنتجات لا تظهر؟
- ✅ تأكد من إضافة البيانات في Supabase
- ✅ تأكد من صحة `.env`
- ✅ اضغط F5 للتحديث

### الصور لا تظهر؟
- ✅ تأكد من أن Storage bucket عام
- ✅ اختبر رفع صورة جديدة

### خطأ في تسجيل الدخول؟
- ✅ تأكد من صحة البريد والكلمة السرية
- ✅ تحقق من اتصالك بالإنترنت

## 📞 المساعدة والدعم

للأسئلة والمشاكل:
- تحقق من console الموقع (F12)
- اقرأ رسائل الأخطاء جيداً
- جرّب تحديث الصفحة

## 📝 الملاحظات المهمة

⚠️ **هام جداً:**
- لا تشارك `NEXT_PUBLIC_SUPABASE_KEY` مع أحد
- استخدم رسائل أخطاء واضحة بالعربية
- احفظ في Git بدون ملف `.env`

## 🎯 التطويرات المستقبلية

- [ ] نظام تقييم المنتجات
- [ ] البحث والتصفية
- [ ] نظام الخصومات والكوبونات
- [ ] تطبيق موبايل
- [ ] لوحة تحكم متقدمة للمدير

---

**مصنوع بـ ❤️ للعملاء العرب**

الدفع عند الاستلام 💳 | شحن سريع 📦 | جودة مضمونة ✅
